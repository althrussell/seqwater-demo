{
  "name": "${APP_NAME}",
  "description": "Seqwater AI Command Centre — Databricks demo for water security, asset risk, water quality, flood readiness, and the AquaIQ governed AI assistant. All data is demo.",
  "resources": [
    {
      "name": "sql-warehouse",
      "description": "Serverless SQL Warehouse for Unity Catalog queries.",
      "_required": ["DATABRICKS_WAREHOUSE_ID"],
      "sql_warehouse": {
        "id": "${DATABRICKS_WAREHOUSE_ID}",
        "permission": "CAN_USE"
      }
    },
    {
      "name": "llm-endpoint",
      "description": "Foundation Model API endpoint powering AquaIQ in non-Supervisor mode.",
      "_required": ["DATABRICKS_LLM_ENDPOINT"],
      "serving_endpoint": {
        "name": "${DATABRICKS_LLM_ENDPOINT}",
        "permission": "CAN_QUERY"
      }
    },
    {
      "name": "supervisor-endpoint",
      "description": "Seqwater Supervisor MAS endpoint (orchestrates KA + Genie + UC functions).",
      "_required": ["DATABRICKS_SUPERVISOR_ENDPOINT"],
      "serving_endpoint": {
        "name": "${DATABRICKS_SUPERVISOR_ENDPOINT}",
        "permission": "CAN_QUERY"
      }
    },
    {
      "name": "ka-endpoint",
      "description": "Seqwater Operational Docs Knowledge Assistant endpoint.",
      "_required": ["DATABRICKS_KA_ENDPOINT"],
      "serving_endpoint": {
        "name": "${DATABRICKS_KA_ENDPOINT}",
        "permission": "CAN_QUERY"
      }
    },
    {
      "name": "genie-space",
      "description": "Seqwater Operations Genie space (also embedded in the app).",
      "_required": ["DATABRICKS_GENIE_SPACE_ID"],
      "genie_space": {
        "space_id": "${DATABRICKS_GENIE_SPACE_ID}",
        "permission": "CAN_RUN"
      }
    }
  ],
  "user_api_scopes": [
    "sql",
    "catalog.tables:read",
    "catalog.schemas:read",
    "catalog.catalogs:read",
    "files.files",
    "serving.serving-endpoints",
    "dashboards.genie"
  ]
}
