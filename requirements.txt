# Runtime requirements for the Seqwater AI Command Centre when deployed as a
# Databricks App. Mirrors the `dependencies` + `databricks` extras in
# pyproject.toml. Pinned to known-good minor versions for reproducibility.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
httpx>=0.27.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0
duckdb>=1.0.0
tabulate>=0.9.0
rich>=13.7.0
python-multipart>=0.0.9

# Databricks integrations (Unity Catalog SQL, Foundation Model API, MLflow tracing)
databricks-sdk>=0.30.0
databricks-sql-connector>=3.3.0
mlflow-skinny>=2.16.0
